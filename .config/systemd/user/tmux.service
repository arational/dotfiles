[Unit]
Description=Start tmux in detached session

[Service]
Type=forking
ExecStart=/usr/bin/tmux new-session -s tmux -d
ExecStop=/usr/bin/tmux kill-session -t tmux

[Install]
WantedBy=default.target
